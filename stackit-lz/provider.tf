# Credentials are read from environment variables, so no values are set here:
#   MESHSTACK_ENDPOINT                        - e.g. https://api.my.meshstack.io
#   MESHSTACK_API_TOKEN                        - API token, or:
#   MESHSTACK_API_KEY / MESHSTACK_API_SECRET   - API key/secret pair
provider "meshstack" {}
